//import { useSelector } from "react-redux";
function SearchResults() {
    //const { searchFor ,result:{data} } = useSelector(store=>store.search);

    return ( <aside className="search-results">
                results
            </aside>);
}

export default SearchResults;